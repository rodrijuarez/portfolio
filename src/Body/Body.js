import React from 'react';
import cssModule from 'react-css-modules';
import styles from './Body.css';

const Body = () => (
  <div>
    Body
  </div>
);

Body.propTypes = {

};

export default cssModule(Body, styles);
